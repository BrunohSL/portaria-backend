version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: cca-mysql
    network_mode: "host"
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_ROOT_PASSWORD:-cca_dev_2024}
      - MYSQL_DATABASE=cca
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: cca-redis
    network_mode: "host"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build: .
    container_name: cca-api
    network_mode: "host"
    environment:
      - PORT=3000
      - DB_HOST=localhost
      - DB_PORT=3306
      - DB_NAME=cca
      - DB_USER=root
      - DB_PASSWORD=${DB_ROOT_PASSWORD:-cca_dev_2024}
      - REDIS_URL=redis://localhost:6379
      - TZ=America/Sao_Paulo
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./src:/app/src
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  mysql_data:
